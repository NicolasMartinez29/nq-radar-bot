{
  "name": "nq-radar-bot",
  "version": "3.1.0",
  "private": true,
  "description": "NQ Radar V3 Predator \u2014 paper-only, TradingView IP allowlist, no feed secret",
  "scripts": {
    "check": "node --check worker.js",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.120.0"
  }
}
